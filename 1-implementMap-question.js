
/* 

    Implement a map function for an array, similar to the .map property found in core JS.
    
    You can implement as a basic method, or extend the prototype if needed. 

    Parameter 1) Data is passed as an array
    Parameter 2) A callback that implements the behaviour

    Note: Consider immutability
    
*/ 
var map = function(data, func){
    // Your code here
};

module.exports = map;
