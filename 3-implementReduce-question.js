
/* 

    Implement a reduce function for an array, similar to the .map property found in core JS.

    Parameter 1) Data is passed as an array
    Parameter 2) A callback that implements the behaviour of reduce
    Parameter 3) The default value for the reduction

    Note: Consider immutability
*/ 
var reduce = function(data, func, default) {

    // Your code here

};

module.exports = map;